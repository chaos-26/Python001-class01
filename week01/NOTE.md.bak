学习笔记

1、beautiful soup
find_all() 方法将返回文档中符合条件的所有tag，find()是返回一个结果。
soup.find_all('title', limit=1)
# [<title>The Dormouse's story</title>]
soup.find('title')
# <title>The Dormouse's story</title>
例如以上两个是等价的。但是使用find()效率高。

2、数组转字符串
    movie_type_str = ','.join(movie_type)

3、CSV相关
mytuple=[(movie_name,movie_time,movie_type_str)]该类写入为列写入
mylist=[movie_name,movie_time,movie_type_str]为行写入
